language: cpp
compiler:
  - clang
addons:
  apt:
    sources:
      - kalakris-cmake
      - llvm-toolchain-trusty
      - ubuntu-toolchain-r-test
    packages:
      - cmake-3.5
      # - clang-4.0
script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build .
